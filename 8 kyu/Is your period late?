// JavaScript
function periodIsLate(last, today, cycleLength) {
  var dayDiff = (today.getTime() - last.getTime())/86400000;
    // 86400000ms = 1 day
  if(dayDiff > cycleLength) {
    return true;
  } else {
    return false;
  }
}
